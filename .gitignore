# ── Node / React (Apps-in-Toss mini-app via create-ait-app + Vite) ──
node_modules/
dist/
.ait/
*.ait
.granite/
.env
.env.local

# ── OS / editor ──
Thumbs.db
.DS_Store
*.log

# transient dev-server capture (PID-scoped runs, see evidence-silhouette.mjs)
.devserver.*

# Gate 3 playtest screenshots (left by playtest runs, not part of QA evidence)
.playtest-shots/
