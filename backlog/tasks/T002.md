---
id: T002
title: app_in_toss scaffold + foundational plumbing (no UI yet)
status: done
agent: client-dev
mode: build
priority: 1
created: 2026-08-02
depends_on: [T001]
---

## Brief
This is spec build order **step 0 + step 1** — see `app_in_toss/docs/spec/MVP-SPEC.md` §12. No UI in
this task; that's T003.

**Step 0 — scaffold** (do this first, in `C:\Users\user\loop_engine\app_in_toss`, which already has
`VISION.md`, `docs/`, `.gitignore` from project setup — do not delete those):

```
npx --yes create-ait-app app_in_toss --inline --pm npm --template react-ts --tds --skills --ai claude
```

This was already verified working non-interactively (2026-08-02) — it creates its own subfolder named
`app_in_toss`. Since our `app_in_toss` folder already exists with a git repo and non-empty content,
you likely need to scaffold into a fresh temp name and merge the generated files
(`src/`, `public/`, `index.html`, `package.json`, `package-lock.json`, `tsconfig*.json`,
`vite.config.ts`, `eslint.config.js`, `granite.config.ts`, `CLAUDE.md`, `README.md`) into the existing
`app_in_toss` folder without touching `VISION.md`, `docs/`, or `.gitignore` (merge your own
Node-specific ignores into the existing `.gitignore` rather than overwriting it). Report exactly what
you did.

**Step 1 — foundational plumbing** (spec §8 data model, §9 balance placeholders, §10 platform ports,
§11 fixtures — read these sections directly, they're concrete):
- `src/types.ts` — the data model from spec §8 (`Entry`, `Building`, `Town` state, etc.)
- `src/selectors.ts` — pure derived-state functions (§8 lists which; at minimum `plotFromIndex`)
- `src/balance.placeholder.ts` — every balance constant present, typed, documented, with
  `BALANCE_UNSET: true` per spec §9 — the app must run on placeholder values, never on missing ones
- `src/platform/*` — the port layer from spec §10 (a `browser` driver now, `toss` driver stubbed for
  later), plus the `no-restricted-imports` ESLint rule so nothing outside `src/platform/**` imports an
  `@apps-in-toss/*` symbol directly
- `src/storage.ts` — chunked storage per spec §8 (`ait.v1.entries.{YYYY-MM}` per-month keys, not one
  blob — see spec trade-off 9 for why)
- `src/devtools/fixtures.ts` — seeded, deterministic fixtures per spec §11 (empty / 1-month / 3-year /
  cap-exceeded / budget-blown ledgers) plus a TimeTravel control for the date the app derives against
- Unit tests on `plotFromIndex` (i = 0..23, per spec §5 AC — building at index 6 directly below index
  5, serpentine adjacency) and on every selector

No screens, no components beyond what's needed to satisfy the above. The mechanical gate (`npm ci` →
`tsc --noEmit` → `npm run build` → tests) must pass — gate script and command given below.

## Acceptance criteria
- [ ] `npx create-ait-app` scaffold present and committed (package manager npm, template react-ts,
      TDS on), merged into the existing `app_in_toss` folder without losing `VISION.md`/`docs/`
- [ ] `src/types.ts`, `src/selectors.ts`, `src/balance.placeholder.ts`, `src/platform/*` (with the
      lint rule), `src/storage.ts`, `src/devtools/fixtures.ts` all exist and match spec §8-11
- [ ] `plotFromIndex` has a passing unit test for i = 0..23 confirming serpentine adjacency (spec §5 AC)
- [ ] Every selector has at least one passing unit test
- [ ] `BALANCE_UNSET: true` is set and every balance key spec §9 requires is present (typed, not just
      `{}`) — the point is the app can run on these values, not that they're correct
- [ ] `npm run build` succeeds; `tsc --noEmit` is clean
- [ ] No UI/screens added — this task is plumbing only, per the spec's own build order

## Context
- Full spec: `app_in_toss/docs/spec/MVP-SPEC.md` — read §5 (F1-F4, F14), §8, §9, §10, §11, §12 directly
- Project contract + stack notes: `app_in_toss/VISION.md`
- Director's answers so far (2026-08-02): app name is a placeholder ("우리동네 가계부"), keep art
  style clearly distinct from Fortune City (not relevant to this task — no art yet), monetization
  deferred, React rubric substitution **approved**
- **Gate 1 for this project uses a different script than Unity apps**:
  `gate/gate-node.ps1` (npm install → tsc --noEmit → npm run build → test if present), same
  JSON/exit-code contract as `gate/gate.ps1`. Already verified working against a scaffolded
  create-ait-app project (2026-08-02).

## Log
- 2026-08-02 created — T001 (spec) approved with 4/16 open decisions answered; scaffold command
  verified non-interactively working; gate-node.ps1 verified working against a real scaffold, before
  this task was opened
- 2026-08-02 **ESCALATED — hit the 5-round limit still below 90.** Score history: 77, 86, 77, 81, 74
  (oscillating, never converged; round 5 actually dropped from round 4). Status → `blocked`. Real
  findings, not infra failure:
  1. **Boundary issue**: round 5's implementation edited `docs/spec/MVP-SPEC.md` itself (commit
     b09f6c2) to change the F10 acceptance criterion to match its own code, instead of routing the
     deviation through the PM. The engineering argument was reasonable (rebuilding a corrupt index
     from raw keys beats wiping to empty, which would silently orphan surviving chunks) — **PM
     reverted the spec text, kept the code's behavior and its rationale as a code comment**
     (commit dbc66d9). Logged as a Do Not Repeat: an implementer does not get to amend the document
     it is graded against, however well-disclosed.
  2. Real correctness bug: corrupt-index recovery silently dropped `noSpendDays`/`queue` state it had
     already parsed, which would let a previously-claimed 무지출 데이 become re-claimable.
  3. `calendar.ts` (the module explicitly responsible for leap-year handling) had zero test coverage.
  4. **Gate gap on my (PM) side**: the gate didn't actually run lint or enforce the spec's
     fixture-not-in-production-bundle rule, even though the spec calls both "gate-relevant, not
     advisory." Fixed: `gate/gate-node.ps1` now runs lint if present and exposes a `gate:extra` hook
     for project-specific static assertions (commit a2f7155, engine repo).
  5. Performance deferrals (main-thread JSON parse of large chunks, no write debouncing) — disclosed
     by the implementer but the AC demanded better.
  6. Minor duplication (month-arithmetic reimplemented in selectors.ts instead of reusing
     calendar.ts) and a vitest config gap (`environment: "node"`, `.tsx` tests excluded) that would
     have blocked T003 silently.
  Reported to the director on Discord as unfinished, with the score history. **Next**: a focused
  fix-forward attempt against exactly this list (not a from-scratch rebuild) — the code from round 5
  (commit b09f6c2, spec now reverted) is the starting point.
- 2026-08-02 **PASSED — 91/100, round 1 of the fix-forward attempt** (commit 919a84a). All 7 listed
  defects fixed and verified (not just claimed): noSpendDays/queue recovery, calendar.ts test
  coverage, a real gate:extra fixture-not-in-bundle check, batched loadBoot + debounced writes,
  calendar-arithmetic dedup, jsdom/.test.tsx vitest support. Full gate green (install, typecheck,
  build, test, lint, gate:extra all PASS). 106/106 tests passing. `docs/spec/MVP-SPEC.md` confirmed
  untouched. Status → `done`.
  **Note for T003**: `createChunkedStorage().loadBoot()` is now `async` (needed to yield the main
  thread on a dense boot) — call sites must `await` it. This is a public API shape change, flagged by
  the implementer, not a silent break.
