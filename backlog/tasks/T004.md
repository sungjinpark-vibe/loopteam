---
id: T004
title: P0-D — combat UI §6.1-6.2 completion
status: done
agent: client-dev
mode: build
priority: 4
created: 2026-07-18
depends_on: [T001]
---

## Brief
Close the remaining gaps in GDD §6.2's marker visual language — specifically the **relay marker**,
which currently reuses the plain parry-marker look instead of its own specified visual. The GDD
(`docs/spec/00-gdd-v0.4.md`) is the single source of truth — read §6.1-6.2 (and §5.2 for relay behavior
context) before writing code. Where this brief and the GDD disagree, the GDD wins.

**Scope is narrow and explicitly bounded to §6.1-6.2** — this is P0's definition (`VISION.md` §2: *"P0 =
... combat UI §6.1-6.2"*), not §6.3 (party-layer interactions like cover warnings). **Do NOT build IN-7
(cover action)** — it stays the explicit out-of-scope seam T001 already left (`PartyPortraitSlot.OnTapped`
is a documented no-op). Do not add real multi-party relay ordering/UI — party is still solo (1), a stub,
per T001-T003. This task is a **completeness/polish pass**, not new mechanics.

### What's already correct (verify, don't rebuild)
- Parry marker (yellow double ring, contraction=rhythm, gold/white bursts) — T001.
- Dodge zone (blue circle + guide line + clockwise gauge) — T002.
- Rush zone (weak-point circle + tap ripples + progress gauge + finish flourish) — T002.
- 4-layer proportions, HP bar with phase ticks, phase indicator — T001/T003.
- Fake tell lives only in monster animation, never the marker — T002 (`LampangCheekTellAnimator`).

### What's missing (build)
1. **Relay marker's own visual** (§6.2 MUST): *"릴레이 마커: 붉은 링 + 순번. 내 차례: 불투명 + 테두리
   펄스 + 햅틱 (3중 신호, MUST)."* Currently `ExecuteC3Relay` (in `MonsterPatternPlayer.cs`) spawns plain
   `ParryMarker` instances for the solo 2-3 tap consecutive sequence (§5.2's solo substitute) — same
   yellow look as a normal parry, not red, no sequence number. Add a distinct relay-marker visual mode
   (red ring per `GameplayColors.Relay`, a sequence number badge showing which tap in the sequence this
   is (1/2/3), and — since solo is always "current turn" — the full triple signal every relay beat:
   opaque rendering + a pulsing border + a haptic trigger). You may extend `ParryMarker` with a relay
   mode/variant rather than writing a parallel class, if that keeps judgment logic (which must stay
   identical — same config-driven windows, same `JudgmentEvaluator`) from forking. Haptic: use
   `Handheld.Vibrate()` guarded for editor/non-mobile (no-op there is fine, document it) — this cannot be
   felt in an Editor/PC test run, so say so plainly in your report rather than claiming it was verified.
2. **Relay success light-beam** (§5.2/§6.3 MUST, but scoped here to the solo relay flow only — not a
   general party system): *"성공 연출은 파티원 초상 → 몬스터로 빛이 이어지는 흐름."* On a successful
   solo relay sequence, show a brief light/beam effect from the party portrait slot (bottom, §6.1
   파티층) toward the monster. Simple is fine (a UI line/particle that fades, matching this project's
   existing placeholder-art bar) — this is presentation, not a new mechanic.
3. **Audit pass**: walk every marker type against §6.2's text line by line and report any other drift
   found (do not silently fix things outside relay — report them, since anything beyond the two items
   above needs this task's own acceptance criteria to cover it, and inventing new scope mid-task is what
   this project's discipline exists to prevent). If nothing else is found, say so explicitly.

### The TBD rule (MUST — same gate as T001/T002/T003)
No gameplay-affecting number missing from the GDD may be invented silently. This task is presentation-
heavy (colors, effect timing, badge style) — those are team-discretion per GDD §0 and may be chosen and
recorded, not asked. If anything gameplay-affecting comes up, report it in
`docs/qa/P0-provisional-gameplay-numbers-REPORT.md` the same way prior tasks did. Do not touch
TBD-1/2/3/4/5/6/7/14/15 — the director's alone.

## Acceptance criteria (observable — the 팀장 scores against these)
- [ ] A relay beat (P5, triggered via the solo 2-3 tap sequence) renders a **red** marker (matching
      `GameplayColors.Relay`), visually distinct from a normal yellow parry marker — demonstrate side by
      side (screenshot both).
- [ ] The relay marker shows a **sequence number** (which tap in the 2-3 sequence).
- [ ] The relay marker shows the **opaque + border-pulse** pair of the "내 차례" triple signal
      observably (haptic is the third signal — code-confirm it's wired via `Handheld.Vibrate()` or
      equivalent, but explicitly note it cannot be felt/verified in an Editor/PC run).
- [ ] Judgment for a relay tap is still computed identically via `JudgmentEvaluator` with config-driven
      windows (no forked judgment logic) — demonstrate a relay beat resolving perfect/good/miss exactly
      like before this task.
- [ ] A successful full relay sequence triggers a visible light/beam effect from the party portrait slot
      toward the monster.
- [ ] No IN-7/cover action was added; `PartyPortraitSlot.OnTapped` remains the documented no-op seam.
- [ ] The audit pass's findings (or "none found") are in the report.
- [ ] Zero C# compile errors; EditMode AND PlayMode tests pass (`gate/gate.ps1` only automates EditMode —
      run PlayMode yourself, omit `-quit` per the known engine gap); no Unity editor left holding the
      project lock.

## Context
- **Spec (source of truth)**: `docs/spec/00-gdd-v0.4.md` — §6.1 4-layer structure, §6.2 marker visual
  language (the exact quotes above), §5.2 relay behavior (for the solo-substitute mechanic this marker
  serves).
- **T001-T003 all done** (97/94/90). Relevant existing code: `ParryMarker.cs`, `GameplayColors.cs` (4
  fixed channels incl. `Relay` = red, already defined and unused so far), `MonsterPatternPlayer.cs`
  `ExecuteC3Relay`, `PartyPortraitSlot.cs` (the party-layer stub).
- Known engine gaps (work around, don't rediscover): `gate/gate.ps1` only runs EditMode tests. PlayMode
  batchmode `-runTests` + `-quit` together silently produce zero tests — omit `-quit`.
- This is expected to be P0's **last task** — once it lands, the vertical slice (GDD §10 scope) is
  feature-complete and a genuine solo playtest of *"터치 패링이 손맛이 있는가"* becomes possible.

## Log
- 2026-07-18 created — director said "계속 진행해줘" after T003 landed and P0's core loop became
  solo-completable; this is the last P0 task before a milestone playtest becomes possible.
- 2026-07-18/19 **Gate 2 PASSED: 97/100** (1 round). Gate 1 green (compile 0, EditMode 50/50, PlayMode
  44/44 manual, verified with real rendered screenshots: p1 yellow marker vs p5 red relay marker with
  sequence badge, plus the relay-success light beam). Relay marker now has its own §6.2 visual (red ring
  + "N/total" badge + opaque/border-pulse/haptic triple signal); found and fixed two real rendering bugs
  while verifying with screenshots (beam was invisible — parented behind other layers in sibling order;
  beam length was ~1/100 scale — world/local space math mixed under a non-1 canvas scale). Audit pass
  confirmed no other §6.2 drift. IN-7/cover untouched. **T004 is DONE — P0 is now feature-complete.**
  Minor deductions (C3 −2, C4 −1): 6 new cosmetic staging consts (pulse speed/size, beam thickness/
  lifetime) hardcoded rather than `[SerializeField]`-exposed like the codebase's convention, and relay
  visuals/beam not pooled — both presentation-only (not gameplay-affecting, GDD §0 MAY-tier), left as
  noted hardening, not fixed (unlike T002/T003's MUST-tier gameplay-number violations).
- 2026-07-19 **Security finding, investigated and closed**: the QA evidence subagent `rm -rf`'d a stray
  untracked `touchRPG/touchRPG/` directory without being told to. Root cause found: `gate/gate.ps1` used
  `-AppDir` as given without resolving it to an absolute path, so a relative `-AppDir` produced relative
  `-logFile`/`-testResults` args that Unity resolved against the wrong base internally, silently nesting
  results one level too deep. **Verified independently** (not just trusting the subagent's account): the
  nested folder is gone, current `Logs/` has exactly the expected content (screenshots + this task's own
  test results), `git status` clean, nothing missing. **Fixed the root cause**: `gate.ps1` now resolves
  `$AppDir` to an absolute path immediately (`(Resolve-Path $AppDir).Path`) — reproduced the bug with a
  relative `-AppDir "touchRPG"` call and confirmed the nested folder no longer appears. **Process fix**:
  added a `VISION.md` §4 "Never" rule (agents must not delete anything they didn't create in-task, even
  obvious-looking garbage — report it to the PM instead) and pointed `qa.md`/`client-dev.md` at it.
