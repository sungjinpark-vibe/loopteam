---
id: T017
title: app_in_toss — harden EntrySheet backdrop fix (fix-forward round 2)
status: in-progress
agent: client-dev
mode: build
priority: 1
created: 2026-08-05
depends_on: [T015]
---

## Brief
T015 passed 90/100 but its team-lead review flagged a third, unfixed instance of the nested-confirm
backdrop-lockup bug in `EntrySheet.tsx` (S4, primary add-transaction flow). A fix-forward round
(commit `da5d291`) wired the shared hook into that site and was sent for verification — **it scored
86/100, FAIL** (bar 90). Real findings, not nitpicks:

1. **C2 −5, a real race condition**: `useConfirmDialogBackdropFix.ts` assumes exactly one
   `[aria-label="닫기"]` node exists at a time ("verified live" — was true for the original two sites,
   false for EntrySheet: 2 nodes while the nested confirm is open). The fix only works today because
   TDS's confirm dimmer happens to unmount within one `requestAnimationFrame`. On a slow device or a
   heavy commit — precisely the condition that produces the original vendor bug — it could walk the
   wrong subtree and silently no-op.
2. **C2 −4**: zero tests bind the hook to `EntrySheet` — deleting the one wired line would still pass
   413/413. The exact class of miss this round exists to fix is not caught by the suite.
3. **C5 −3**: `npm test` (the command the gate actually runs) is nondeterministic at HEAD — 3
   failures across ~16 runs at `useTownStore.history.test.tsx:250` (`ensureMonthLoaded` asserted
   synchronously without awaiting the load, line 249). The `--no-file-parallelism` workaround used to
   get a clean run was never committed. Not caused by `da5d291`, but it means the gate itself can't be
   trusted to catch a real regression.
4. **C1 −2**: `useConfirmDialogBackdropFix.test.tsx`'s docblock still says "for both EntryDetailSheet
   and SettingsSheet" — stale, now three call sites.

## Acceptance criteria (per the lead's own topFix, in this order)
- [ ] Add `EntrySheet.test.tsx`: render the sheet open, touch a field, fire the dimmer click, cancel
      계속 입력, advance a frame, assert no `inert` ancestor remains above `[aria-label="닫기"]` — a
      test that fails if the wired hook call is removed
- [ ] Make `useConfirmDialogBackdropFix`'s dimmer lookup safe against the 2-dimmer window — scope it to
      the sheet's own backdrop (pass by ref, or otherwise deterministic) instead of a bare
      `document.querySelector('[aria-label="닫기"]')`. Extend
      `useConfirmDialogBackdropFix.test.tsx`'s `mountStuckDom` to mount two matching nodes (the shape
      measured live) and assert the hook still picks the right one
- [ ] Fix the missing `await` at `useTownStore.history.test.tsx:249-250` (or the actual root cause of
      that flake) so `npm test` — no CLI flags — is deterministic; if file-parallelism is genuinely the
      cause, commit that setting in `vitest.config.ts` rather than relying on an uncommitted flag
- [ ] Update `useConfirmDialogBackdropFix.test.tsx`'s stale docblock to list all three call sites
- [ ] `npm test` run **3 times in a row with the exact command the gate runs** (no added flags), all
      green, before calling this done
- [ ] gate green (`gate/gate-node.ps1`), no changes outside `app_in_toss/src`, `docs/spec/` and
      `balance.placeholder.ts` untouched

## Context
- Prior round: `backlog/tasks/T015.md`, commits `1492757`, `da5d291`
- `src/hooks/useConfirmDialogBackdropFix.ts`, `src/components/EntrySheet.tsx`,
  `src/useTownStore.history.test.tsx`

## Log
- 2026-08-05 created after fix-forward round 1 (da5d291) scored 86/100 FAIL on team-lead review.
  Full findings copied into Acceptance criteria above. Starting round 2 immediately per the lead's
  topFix.
