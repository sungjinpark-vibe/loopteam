---
id: T005
title: Art mockups for director sign-off (village/timer/receipt)
status: ready
agent: ui-ux
mode: —          # direct deliverable, not run through quality-loop (director is the judge of a mockup, not a grader)
priority: 1
created: 2026-07-16
depends_on: []
---

## Brief
Round 1 (done): built 3 self-contained HTML mockups (village/timer/receipt) from the T004 art design
system (`lifetown/docs/design/00-art-design-system.md`), in the director's "1과 2가 적절하게 섞여야해"
(cohesion + readability+delight blend). Rendered to PNG via `render-html.ps1` + Chrome (390x844 @2x),
committed to `lifetown` (454ea03), sent to Discord for sign-off.

Round 2 (now): revise the village mockup per director feedback below. Timer and receipt mockups were
not called out — do not touch unless the building fix requires shared style changes.

## Director feedback — 2026-07-16 21:17 (Discord msg 1527288064743968919)
Quoted verbatim: "1번 조정이 필요해. 일단 이미지엔 건물이 안보여. 누가봐도 건물스럽게 리소스가 나와야해.
그리고 종류도 다양해야해."

Translation: "Option 1 [the village mockup] needs adjustment. Right now the image doesn't read as
buildings at all. The resources need to unmistakably look like buildings to anyone looking at them.
Also there needs to be more variety of building types."

This is an **adjustment request, not approval**. Do not treat it as a green light for the App-layer
Unity screen build — that only happens once the director approves the visual direction (👍).

## Acceptance criteria
- [ ] Village mockup buildings are unambiguously readable as buildings at a glance (clear roof/wall/
      window silhouettes, not abstract blocks or icons) — this is a taste call the director makes on
      the re-rendered PNG, not something the team self-certifies.
- [ ] Multiple distinct building types/silhouettes are visible (varying height, footprint, roofline —
      not the same shape recolored per category).
- [ ] Category color coding (T004 spec) and isometric tier/construction-ring mechanics stay intact —
      this is a legibility fix, not a redesign of the system.
- [ ] Re-rendered PNG sent to Discord with a short 👍/🔧 prompt, same as round 1.

## Context
- Source: `lifetown/docs/design/mockups/village.html` (+ `.png`), also `timer.html`/`receipt.html` for
  shared style reference only.
- Design system source of truth: `lifetown/docs/design/00-art-design-system.md` (T004, passed 92/90).
- Render pipeline: `C:\Users\user\app-dev-team\.telegram\render-html.ps1` + headless Chrome
  (`C:\Program Files\Google\Chrome`), 390x844 @2x — already verified working.
- Send via `.discord\send-file.ps1` (images render inline).

## Log
- 2026-07-16 Tick 11-12: built + rendered 3 mockups, sent for sign-off, status → awaiting-approval.
- 2026-07-16 21:17 (Tick — scout): director adjustment feedback received on the village mockup. Status
  → `ready` for a round-2 revision. Task file created (did not previously exist; backlog row only).
