---
id: T016
title: app_in_toss — balance pass (director-approved values, flip BALANCE_UNSET)
status: ready
agent: client-dev
mode: build
priority: 1
created: 2026-08-05
depends_on: [T015]
---

## Brief
Implement MVP-SPEC.md §9's balance pass. The director approved real values for `balance.placeholder.ts`
(Discord, 2026-08-05, msgs 1534906419516538891 / in-session reply). This is the last prerequisite
before Gate 3 (the 5-expert playtest that ends app_in_toss development) can legally run — §9 rule 2
forbids Gate 3 while `BALANCE_UNSET === true`.

**T015/T017 are done** (93/100, 4 rounds, all committed/pushed on `app_in_toss`) — this task is now
unblocked. `HistoryScreen.tsx` was one of T015's touched files; that work is fully landed, so no
file-race risk remains.

**Per §9 rule 3: never edit `balance.placeholder.ts` in place.** Create a new `src/balance.approved.ts`
with the exact same shape (`BALANCE_UNSET: false` plus every field), so the diff between the two files
is the permanent record of what the director actually decided. `balance.placeholder.ts` itself must
stay byte-identical to what it is today — it's now historical record, not live config.

**Approved values** (all confirmed by the director, 2026-08-05):

| Field | Placeholder (old) | Approved (new) | Note |
|---|---|---|---|
| `BALANCE_UNSET` | `true` | `false` | — |
| `dailyBuildSlots` | `5` | **`10`** | Director's explicit change. Stated future intent (not built now): building past 10/day requires a paid unlock — monetization system itself is separate R&D (spec D-7), out of scope for this task. Do not build any paywall UI or payment hook — just ship the flat cap of 10. |
| `materialQueueMax` | `10` | `10` (unchanged) | PM judgment call — this dial was accidentally left out of the question sent to the director (D-14). Flagged to the director; defaulting to no change since every other dial they didn't want to touch was left at default. Revisit if the director objects. |
| `tierThresholds` | `[0, 10, 30, 80, 200]` | unchanged | Director: keep default |
| `moodPaceThresholds` | `[0.9, 1.1]` | unchanged | Director: keep default |
| `variantsPerCategory` | `3` | unchanged | Director: keep default |
| `savingsTowerSegments` | `[100_000, 300_000, 600_000, 1_000_000, 2_000_000, 4_000_000, 7_000_000, 10_000_000]` | unchanged | Director: keep default |
| `savingsStructureSegments` | `{}` | unchanged | No per-structure override requested |
| `noSpendDayCostsSlot` | `true` | unchanged | Director: keep default |

**Wiring**: every current import of `BALANCE` from `./balance.placeholder` (13 sites as of T014 — grep
`from ["']\.\./?balance\.placeholder` across `src/`) must resolve to the approved values at runtime.
Pick whichever mechanism is cleanest (switch each import site to `./balance.approved`, or add one
indirection point) — just don't leave two disagreeing sources of truth wired into different parts of
the app. `balance.placeholder.test.ts`'s existing assertion (`BALANCE_UNSET` must be `true`) stays
pointed at the placeholder file specifically — it's now a regression guard that the historical
placeholder was never mutated. Add an equivalent test on `balance.approved.ts` asserting
`BALANCE_UNSET === false` and the exact approved values above.

**Visible effect**: `App.tsx`'s persistent "밸런스 미승인 — 임시 수치" dev banner must stop showing,
since it's gated on `BALANCE.BALANCE_UNSET` and that's now `false` in the live app.

## Acceptance criteria
- [ ] `src/balance.placeholder.ts` is byte-identical to its current committed state (verify via `git diff`
      showing no changes to this file)
- [ ] `src/balance.approved.ts` exists with exactly the approved table above, `BALANCE_UNSET: false`
- [ ] The live app (verified in a browser) no longer shows the "밸런스 미승인" banner
- [ ] `dailyBuildSlots` is actually 10 in live behavior — verified by building 10 in one day and
      confirming the 11th queues (F14 behavior unchanged, just against the new cap)
- [ ] All existing tests updated to expect the approved values where they assert live app behavior
      (not the placeholder file's own historical values), and pass
- [ ] `npm run build` succeeds, `tsc --noEmit` clean, gate green (`gate/gate-node.ps1`)
- [ ] No regression: docs/spec/ untouched, nothing outside `app_in_toss/` modified, all other MVP
      features (S2/기록/settings/move/savings) still work

## Context
- Full rule: `app_in_toss/docs/spec/MVP-SPEC.md` §9, open decisions D-3/D-4/D-5/D-13/D-14/D-15 (§13)
- Current placeholder: `app_in_toss/src/balance.placeholder.ts`
- Director's approval: Discord msg 1534906419516538891 (PM's question) + in-session reply, 2026-08-05
- Project contract: `app_in_toss/VISION.md`
- **After this passes**: the app is unblocked for Gate 3 (`playtest.js`) — the milestone that ends
  app_in_toss's development phase.

## Log
- 2026-08-05 created — director approved balance values (raised dailyBuildSlots to 10, rest default).
  Set `blocked` on T015 to avoid a HistoryScreen.tsx edit race; PM will flip to `ready` once T015
  lands.

## Log (cont.)
- 2026-08-07 **passed 90/100 after 1 round.** balance.approved.ts created with the exact approved
  table, balance.placeholder.ts confirmed byte-identical (empty git diff). All 13 live import sites
  re-pointed. Live-verified: banner gone, "남은 건축 슬롯 10/10" visible, 11th entry in one day queues
  (F14 unchanged, new cap). Also found and fixed 2 test files that had hardcoded the old
  dailyBuildSlots=5 inline instead of importing BALANCE (useTownStore.test.tsx,
  useTownStore.history.test.tsx) — these weren't caught by the original "13 import sites" grep since
  they never imported BALANCE at all. 417/417 tests, gate green. Committed app_in_toss e99e0d1, pushed.
  **T016 done. MVP-SPEC.md §9's Gate 3 prerequisite is now clear — proceeding to Gate 3.**
