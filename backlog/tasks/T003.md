---
id: T003
title: app_in_toss first vertical slice — town view + entry sheet (the loop closes here)
status: done
agent: client-dev
mode: build
priority: 1
created: 2026-08-02
depends_on: [T002]
---

## Brief
Spec build order (§12) step 2: **F1+F2+F3 = the loop closes here. First demoable milestone.**
Implement S2 (town view) and S4 (entry sheet) using the plumbing from T002
(`types.ts`/`selectors.ts`/`balance.placeholder.ts`/`storage.ts`/`platform/*`/`devtools/fixtures.ts` —
all already exist, read them before writing anything new).

Read directly from `app_in_toss/docs/spec/MVP-SPEC.md`: **F1** (§5, entry sheet fields/validation),
**F2** (§5, building placement incl. the exact `plotFromIndex` contract — already implemented in
`selectors.ts` from T002, do not reimplement), **F3** (§5, town view), **S2 and S4** (§6, screen
specs), and §6.1's `PlaceholderBuilding` note: *"the MVP ships PlaceholderBuilding (rounded rect in
the category colour + category icon + a variant-driven roof shape) behind the same interface as the
final sprite component."* No real art exists yet (director hasn't placed the art order — D-12 art
style question is still open) — build `PlaceholderBuilding` now, behind a real component interface,
so swapping in real art later is a one-component change.

**Scope for this task — empty and normal states only.** S2/S3's `dense` (3-year fixture,
virtualization, <1s paint on ~5,400 buildings) is real but is its own later polish task — do not block
this milestone on it; note what you deferred. 저축/F13, F14 queue, F15 무지출 데이, F5 tier, F16
monuments, F17 memo chips are NOT in scope — those are step 3+ per the build order. This task is
specifically: log a 지출 or 수입 entry → a building appears in the town → reload → it's still there.

**API note from T002**: `createChunkedStorage().loadBoot()` is `async` — call it with `await`.

## Acceptance criteria
- [ ] S4 entry sheet: type (지출/수입 segmented — 저축 option can be hidden/disabled, out of scope),
      amount (numeric input, KRW formatting), category (icon grid filtered by type), date (defaults
      today), memo (optional). 저장 disabled until amount > 0 and a category is chosen.
- [ ] Saving an entry with slots remaining creates exactly one `Building` via the existing
      `plotFromIndex`/placement logic from T002 — no reimplementation
- [ ] S2 town view renders the grid: occupied plots show `PlaceholderBuilding` (category colour +
      icon + variant-driven roof shape), empty plots show an empty lot; new buildings animate in
- [ ] S2 empty state: 0 buildings → "첫 지출을 기록하면 첫 건물이 생겨요" with an arrow to the FAB
- [ ] Header shows at minimum: town name, building count, slot counter
- [ ] Hard reload → the town, entries, and slot count are all identical (uses T002's storage/boot —
      remember `loadBoot()` is async)
- [ ] `npm run build` succeeds, `tsc --noEmit` clean, existing tests still pass, gate green
      (`gate/gate-node.ps1`, includes lint + gate:extra)
- [ ] No regression: `docs/spec/MVP-SPEC.md` untouched (still off-limits — flag deviations in your
      report, never edit it yourself)

## Context
- Full spec: `app_in_toss/docs/spec/MVP-SPEC.md` §5 (F1/F2/F3), §6 (S2/S4), §6.1 (PlaceholderBuilding)
- T002's plumbing: `app_in_toss/src/{types,selectors,balance.placeholder,storage}.ts`,
  `src/platform/*`, `src/devtools/fixtures.ts` — read before writing new code
- Project contract + approved rubric substitution: `app_in_toss/VISION.md`
- `docs/spec/MVP-SPEC.md` is READ-ONLY for implementers (T002's escalation: an implementer edited it
  to match its own code, which was reverted and is now an explicit Do Not Repeat —
  `state/PROGRESS.md`)

## Log
- 2026-08-02 created — T002 (foundational plumbing) passed 91/100; this is the first task that
  produces anything visible/demoable
- 2026-08-02 **PASSED — 92/100 after 5 rounds** (history: 76, 84, 77, 89, 92; commits under
  `app_in_toss`, final at 043b60c). Loop verified end to end in a real browser: empty state → log
  entry → building appears → hard reload → persists. Gate green (install/typecheck/build/test/lint/
  gate:extra all PASS). `docs/spec/MVP-SPEC.md` confirmed untouched throughout.
  **Follow-ups for a later task, not blocking**:
  - Pre-existing UI bug found by QA (not introduced/fixed this round): tapping the entry sheet's
    backdrop a second time after dismissing "계속 입력?" doesn't reopen the confirm dialog — a TDS
    `ConfirmDialog`/`BottomSheet` interaction quirk.
  - S2's bottom tab bar (우리 동네 / 기록 — 기록 tab doesn't exist yet, that's a later step) and
    scroll/auto-scroll-to-newest behavior at real depth weren't exercised — flagged, not deducted
    since 기록 is out of this task's scope.
  - Bundle size: 1.27MB / 405KB gzip, mostly the TDS/Apps-in-Toss SDK boot barrel — already
    manual-chunked, re-measure once real routes exist.
