# app_in_toss — 가계부 마을 (토스 인앱 웹게임)

- 스펙·설계: `docs/spec/` (MVP-SPEC.md + ADDENDUM-01~08). 지도 기하는 **ADDENDUM-08이 최신** — 20×20 고정 맵, 공원/호수 건축 불가 지형, 1×1~2×2 다중 칸 건물. ADDENDUM-06/07의 8컬럼·블록 마스킹 체계는 여기서 대체됨(계단식 톤·명당·들쭉날쭉 외곽은 새 격자에 재적용).
- 플레이 가이드: `docs/PLAY_GUIDE.md` · 실행: 루트 `play.bat`
- QA 증거(스크린샷·검증): `docs/qa/`
- 팀 프로세스·백로그: `../state/PROGRESS.md` (loop_engine)
- 상시 결정: F16(월말 정산·기념비)은 사용자 승인 기능 — 삭제 금지. 실결제(캐시충전)는 스텁 유지, 토스 결제 연동 전까지 성공 경로 금지.
- **맵 디자인 확정 (사용자 승인 2026-08-11)**: ADDENDUM-08의 20×20 맵(공원·호수 배치, 다중칸 건물, 현재 비주얼)이 사용자가 "딱 좋다"고 확정한 기준선이다 — 커밋 afc7cd6의 룩을 사용자 승인 없이 변경·회귀 금지. 후속 작업(건물 아트 디테일, 거리 소품 등)은 이 프레임 안에서만.
- **⚠️ 건물 아트 원칙 개정 (사용자 직접 지시 2026-08-13)**: 사용자가 **"레벨이 오를수록 건물이 높아져야 한다"**, **"무슨 건물인지 알 수 있게 지붕에 간판을 달아라"**를 지시했다. 이는 ADDENDUM-11 §4.0의 **"커지지 않고 고급화" 원칙을 사용자 본인이 폐기·개정**한 것이며, 아래 d8ce379 동결 기록도 이 지시만큼 개정된다. **동결선 위반이 아니라 권한자의 기준 변경이다 — 다음 세션은 이 높이 성장·지붕 간판을 회귀로 오인해 되돌리지 마라.** 개정 후 불변 조건: 건물의 **바닥 풋프린트는 여전히 자기 칸을 채우고 칸을 넘지 않는다**(가로 방향 d8ce379 속성 유지). 높이만 칸 위로 솟는다. 재질·크라운 고급화 채널은 2차 신호로 유지.
- **건물 아트 확정 (사용자 승인 2026-08-12, 위 2026-08-13 지시로 높이 부분 개정됨)**: 커밋 **d8ce379**(건물이 자기 칸을 가로·세로 모두 채움 — 풋프린트별 viewBox 종횡비 = 타일 종횡비, 실측 채움률 4종 전부 94%±0.5pp)가 사용자가 "지금 느낌 좋아"로 확정한 기준선이다. **afc7cd6(맵) + d8ce379(건물 아트) 두 커밋의 룩을 사용자 승인 없이 변경·회귀 금지.** 이 승인은 앞선 지시 *"모든 칸에 꽉 들어차는 건물 이미지를 만들어줘"*의 결과물이며, 칸을 채우면 건물 인상이 바뀐다는 점(1×1이 통통해지고 2×1이 낮고 길어짐)을 사용자가 스크린샷으로 보고 수용한 것이다 — 나중에 "건물이 예전보다 뚱뚱하다"는 이유로 되돌리지 마라. 회귀 방어: `PlaceholderBuilding.test.tsx`의 풋프린트별 채움률 ≥90% 테스트(4 풋프린트 × 2 지붕 형태)와 `scripts/evidence-art-fill.mjs` 실측 하네스. 칸 크기 상수는 `townLayout.ts`(MIN_TILE_WIDTH_PX/TILE_HEIGHT_PX/GRID_GAP_PX) 한 곳에서 나오며 아트가 이를 참조한다 — 격자 상수를 바꾸면 아트도 따라 움직인다.
- **건물 아트 키라인 (2026-08-15, 동결선 안의 품질 고도화)**: 벽·지붕·창문·문에 중립 회색(`grey800`) 외곽선을 넣었다. **취향이 아니라 실측 대응이다** — 14개 카테고리 지붕 중 8개가 자기가 닿는 지형색과 대비 1.5:1 미만이었고(카페 #ffa927 vs 도로 #b9bec6 = **1.03**), 창문은 흰 벽 병원에서 1.14:1로 사실상 보이지 않았다. 키라인은 모든 지형색에 대해 **5.15:1 이상**이라 배경이 뭐든 실루엣이 분리된다. 지형색·카테고리색은 **하나도 바꾸지 않았다**(afc7cd6·d8ce379 유지). 융합 재질 10단계 중 4개가 특정 카테고리 지붕과 완전히 같은 색이라 그 카테고리에선 융합이 아예 안 보이던 버그도 같이 고쳤다(같은 색조 내 빈 단계로 이동, 이제 충돌 0). **기하는 한 점도 안 움직였다** — 골든 스냅샷 diff가 `stroke` 속성 추가뿐이고 `points`는 전부 바이트 동일. 회귀 방어: `buildingArt.contrast.test.tsx`(렌더된 SVG에서 측정, 35 테스트) + `scripts/check-art-contrast.mjs`. 증거: `docs/qa/evidence-art-keyline/`.
- **토큰 절약 (파일럿 2026-08-11)**: 코드 탐색·영향 분석 전에 `code-review-graph` MCP(ToolSearch로 로드)로 blast-radius를 먼저 조회하고, 그래프가 짚어준 파일만 읽어라. 넓은 Glob/Grep 탐색은 그래프 조회 실패 시에만.
- **디자인 결정 시 `ui-ux-pro-max` 스킬 활용 (HQ 지침 2026-08-13)**: 새 UI 컴포넌트·색상 팔레트·타이포·아이콘·모션 결정 전에 이 스킬의 DB를 먼저 조회한다 (React/Vite 스택 규칙·접근성 대비 검증 포함). 차트·대시보드 작업은 `dataviz` 스킬과 병용. 단 확정 동결선(맵·건물 아트)은 이 스킬 권고보다 우선한다.
