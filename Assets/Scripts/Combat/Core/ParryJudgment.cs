namespace TouchRPG.Combat.Core
{
    /// <summary>GDD §3/§4.3: the two parry outcomes plus the implicit third (miss).</summary>
    public enum ParryJudgment
    {
        Perfect,
        Good,
        Miss
    }
}
