---
id: T010
title: Playable gameplay slice — tap building → timer → growth/build
status: ready
agent: client-dev
mode: build
priority: 2
created: 2026-07-19
depends_on: [T009]
---

## Brief
Turn the static village v2 into the first playable slice of Life Town's core loop: tap a building →
run a (focus) timer session → session settles via LifeTown.Economy.Core → EXP/coins → building
growth/level shown in the village. Wire the three finished foundations into the village scene:
- LifeTown.Economy.Core (T002, 99pt): EconomyConfig, Settlement, session math — pure C#, already tested
- LifeTown.Platform (T003, 99pt): Android-safe clock + save-file IO — sessions must survive app restart
- Art design system (T004, 92pt): docs/design/00-art-design-system.md — colors/type/spacing;
  bake the 7 category hex into a CategoryDef ScriptableObject (Core does NOT have colors — known trap)

## Acceptance criteria
- [ ] Tap a building → timer UI starts; finish (or debug-skip) → settlement runs → EXP/coin gain visible
- [ ] Building growth/level visibly changes in the village after enough sessions
- [ ] State persists across app restart (Platform save IO actually used)
- [ ] Timer/economy numbers come from EconomyConfig, not hardcoded (D5 curve [0,60,240,720,2000], D9 leisure ×1.0)
- [ ] Gate green; existing 81 tests intact + new tests for the wiring layer
- [ ] QA evidence: a real run start-to-settlement with screenshots

## Context
- Spec: lifetown/docs/spec/00-mvp-spec.md + 01-decisions-resolved.md (newer wins)
- Identity: life-logging, leisure counts too (07-decisions-locked.md) — not a productivity-only app
- Director approved this direction 2026-07-17 ("실제 게임 동작 진행해줘"), reconfirmed 2026-07-19
- Full quality-loop workflow (novel work — not frugal path). Rubric: VISION.md §3.2 C1-C5

## Log
- 2026-07-19 created; sequence per director: polish (T009) → this → build delivery (T011)
- 2026-07-19 round 1: PASS 93/100. Gate green 92/92 tests (81 pre-existing + 11 new). Tap->timer->settle
  ->growth wired via new SaveGameService (plain C#, no MonoBehaviour). CategoryDef ScriptableObject
  bakes the 7 category hex colors from CategoryPalette (Economy.Core still has none, as documented).
  QA evidence: two 25-min debug-skip sessions on Reading, wallet 0->3000 coin, Lv1->Lv5, save/reload
  round-tripped identical. Screenshots: docs/qa/village-loop-before.png / -after.png.
- Minor deductions (not blocking): interactive tap->OnGUI path itself not exercised by the QA script
  (it calls SaveGameService directly); one edit-mode Object.Destroy warning at BuildingTapTarget.cs:47;
  RebuildPips lacks an Application.isPlaying guard. Left as noted hardening.
- UI is IMGUI/OnGUI placeholder (no uGUI/TextMeshPro package installed, no Korean font) — flagged as a
  follow-up UI task, not a wiring gap.
- Infra note: new SpikeVillageLoopDemo.unity (114MB) wasn't covered by .gitattributes' LFS rule (only
  two exact scene paths were listed) -> first push rejected by GitHub's 100MB limit. Fixed by widening
  .gitattributes to `*.unity filter=lfs` (was two hardcoded paths) and amending the unpushed commit.
  Pushed clean: lifetown 58958dc.
