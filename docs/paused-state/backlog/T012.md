---
id: T012
title: Art resource upgrade — Blender-modeled village landmark
status: blocked
agent: ui-ux
mode: build
priority: 1
created: 2026-08-01
depends_on: []
---

## Brief
Director directive (2026-08-01, in-session): "아트 고도화부터 먼저 진행해줘. 로컬에 블렌더를 설치했어.
그걸 이용해서 아트 리소스 고도화 진행해줘." Local Blender is installed and its MCP connection is live
(verified via `get_scene_info`). Use it to build the game's first genuinely higher-fidelity 3D art
asset — a step up from the ProBuilder primitives used for the 7 category buildings (T006/T007).

Target: model and texture **one landmark prop** for the village plaza — the first realization of the
**Landmark scope item (D11)**, which the director explicitly kept in scope when the spec was approved
(`lifetown/VISION.md` §2 scope row) but which has never been built. A landmark fits Life Town's
identity ("성취를 눈으로 확인" — visible achievement) better than another category building: it should
read as a shared, central piece the village is built around, not one more house.

Respect the established design system (`docs/design/` — palette, style tokens from T004) and the
project's identity (`VISION.md` §2). Do not invent new gameplay meaning for the landmark (e.g. don't
wire it to a progress mechanic) — that is a design decision for `planner`/the director, out of scope
here. This task is the physical asset only.

## Acceptance criteria
- [ ] Landmark modeled in Blender with real materials/textures applied — not the default gray/checker
- [ ] Exported in a Unity-importable format (FBX or glTF) and saved under `lifetown/Assets/Art/Blender/`
- [ ] `.blend`/`.fbx`/`.glb` extensions are covered by `lifetown/.gitattributes` LFS rules before commit
      (already added 2026-08-01 — verify, don't assume)
- [ ] At least 3 viewport/render screenshots from different angles, saved as PNG with reported paths,
      showing clear geometric/material detail beyond what the ProBuilder buildings have
- [ ] Design-system tokens (color palette, style) from `docs/design/` are visibly respected
- [ ] A short design note under `docs/design/` explains the form choice and how it maps to D11's intent
- [ ] Nothing outside `lifetown/` touched; Gate 1 stays green (no C# compile impact expected — this is
      an asset-only change)

## Context
- `lifetown/VISION.md` §2 — scope table keeps D11 (landmarks) in scope
- `docs/design/` — existing design-system tokens (colors/type/spacing) from T004
- `docs/design/village-lineup-7buildings-v2.png` — what the existing ProBuilder buildings look like,
  for the fidelity bar this should clear
- Evidence step: since this asset is not yet wired into a playable scene, QA's evidence pass should
  open and describe the exported render screenshots and asset files directly — do not attempt to launch
  Unity/PlayMode for this task, there is nothing new to run yet.
- Rubric: 아트팀장 (VISION.md §3.2), pass mark raised to **95** for this task only (director's explicit
  instruction, same rubric criteria — a director-directed bar raise, not a rubric edit under §4).
- On pass: PM sends the screenshots to the director on Discord (director's explicit ask) — not this
  agent's job, don't send anything yourself, just report the paths.

## Log
- 2026-08-01 created from in-session director directive.
- 2026-08-01 **Ran 5 rounds via quality-loop, hit the round limit still far below the 95 bar — blocked,
  not done.** Score history: r1-2 evidence-fail (nothing produced), r3 38/100, r4 44/100, r5 could not
  run (session usage limit, resets 1pm Asia/Seoul — external, not this task's fault).
  - **Root cause of r1-3's near-total failure**: `mcp__blender` was added to `ui-ux.md`'s `tools:` line
    mid-session (this same PM session) — but three independent ui-ux subagent rounds each freshly
    re-checked their own tool list and confirmed `mcp__blender__*` was simply not present ("No such tool
    available"). A `tools:` frontmatter edit does not propagate to subagents spawned later in the same
    running session — same class of bug as the ponytail-plugin lesson. **Needs a session restart**
    before this can work at all; more rounds against the same session cannot fix it.
  - **r4 real progress despite the gap**: the ui-ux agent, still without Blender access, wrote
    `Assets/Art/Blender/build_landmark.py` blind (never executed it — no way to). QA's evidence step
    (QA has Bash/PowerShell) ran it headless via the local Blender binary itself to have something to
    observe, and it mostly worked: real geometry, 3 renders, `.fbx`/`.glb`/`.blend` exports all now
    exist on disk (uncommitted, kept as WIP for the next attempt — not the passing deliverable).
  - **아트팀장's r4 deductions are a real, usable fix list** for the next round, not noise: (1) the
    concept's signature element (open-arch belfry + beacon core) is invisible in every render — the
    camera azimuths (45/135/-45, all corner views) can't frame it; needs a face-on angle. (2) the
    exported `.glb` has no `baseColorFactor` on 5 of 13 materials (imports as white in Unity) — the
    procedural shader nodes were never baked to an image before export. (3) no UV unwrap / texture
    anywhere, only flat color fills. (4) bunting flags float detached from the model, no connector
    geometry. Full itemized list in the workflow journal (`wf_7b4e06ae-a87`) if resuming.
  - **Also attempted and blocked**: adding `Bash, PowerShell` to `ui-ux.md` as a Blender-execution
    fallback that doesn't depend on MCP binding — **blocked by the permission classifier** (shell access
    grant needs the director's explicit call). Not done; `ui-ux.md` documents this as pending, not
    granted.
  - **To resume**: (1) restart this session so the `mcp__blender` grant actually binds, (2) decide
    whether ui-ux also gets Bash/PowerShell (director's call) or stays MCP-only, (3) hand the r4 script
    + the 아트팀장 fix list above to the next attempt — it should not start from zero.
  - Reported to the director on Discord, honestly, as unfinished (VISION.md §5).
- 2026-08-01 **Resumed after session restart + director-approved Bash/PowerShell grant. Ran 5 more
  rounds, real progress, second escalation — still blocked, not done.** Score history this resumption:
  60 → 55 → 49 → 50 → 66 (never <95). Combined with the first attempt's 44, the task has now consumed
  **10 rounds and ~2.96M subagent tokens across two full quality-loop runs** and is still 29 points
  short.
  - **This time it actually ran.** mcp__blender + Bash/PowerShell were both live; the agent executed
    the real Blender build headlessly and inspected real output (rendered PNGs opened as images, pixel
    values sampled with PIL, the exported `.glb` parsed as binary JSON) instead of reasoning from the
    script. Committed incrementally itself (`git commit`) each round — reflog shows 4 real WIP commits
    (`5954835`, `7ac14f4`, `b02632a`, `37b0c87`) plus this task's final round, `4982f7a`.
  - **What's actually wrong at 66/100, per 아트팀장's final review**: pixel-level material clipping (the
    gold "achievement" collar and the secondary-hue gem both render blown/clipped instead of the
    authored hex — HSV value/channel clipping from emission or exposure), the 7-category bunting ring
    still isn't legible in either of its two dedicated detail shots (flags occluded by the base
    geometry in both), four factual mismatches between the design note's prose and what the delivered
    renders actually show, and — per A5 — the form is still visibly a stack of Blender primitives
    (cube/cylinder/pyramid/torus/icosphere), which keeps costing originality points regardless of how
    well the materials read.
  - **⚠️ SECURITY WARNING surfaced by the harness on round 1's evidence step**: the QA subagent ran
    `git checkout --` in the lifetown repo, reverting uncommitted changes to files it didn't create,
    including `Assets/LifeTown.App/Scenes/SpikeWork.unity` and two `ProjectSettings` files — files
    outside this task's scope, the same class of violation as the 2026-07-18 unauthorized-`rm -rf`
    incident (`VISION.md` §4 Never rule). **Investigated before doing anything else. No actual data was
    lost**: `git lfs status` shows `SpikeWork.unity`'s committed LFS hash exactly matches the working
    tree (`2c13e50 -> 2c13e50`) — the huge `git diff --stat` line count for it (`211619` deletions) was
    a **false alarm caused by comparing the file's LFS pointer text against its smudged working-tree
    content**, not a real change; the `ProjectSettings` diffs are the same few-line Unity editor
    auto-touch noise present as uncommitted, pre-existing, and unrelated to any project work all
    session. Still logging this as a real process gap, not dismissing it — see engine
    `state/PROGRESS.md` Do Not Repeat.
  - **Not relaunching a third full quality-loop without the director's call.** Two full escalations,
    10 rounds, ~3M tokens, and the score trajectory this round (60→55→49→50→66, oscillating rather than
    converging) suggests the remaining gap may not be a "keep revising the same approach" problem —
    worth considering a different base (e.g. a Sketchfab/PolyHaven-sourced mesh via the same Blender MCP
    integration, instead of hand-authored primitives that keep losing A5 originality points) rather than
    another 5 rounds of the same script. Reported to the director with the full history and this
    recommendation; awaiting direction.
- 2026-08-01 **Director's answer: "포춘시티 건물과 똑같은 느낌으로 만들었으면 좋겠다"** (want the Fortune
  City look) — a concrete style target rather than continue/pivot/stop as framed. Ran a cheap research
  pass (single agent, ~69K tokens, not a full quality-loop) before spending more on another attempt:
  - Extracted a concrete, checkable style brief (genre-verified chibi-isometric mobile-tycoon
    convention — squat proportions, one dominant steep roof per volume, flat/2-tone 8-12 color palette,
    2-3 discrete toon shading bands, rounded eave/frame bevels). This directly targets both prior
    failure modes: flat/toon shading doesn't depend on scene lighting to read correctly (fixes the
    color-clipping deductions), and the stylization itself reads as deliberate rather than "default
    primitive stack" (targets A5).
  - Checked Hyper3D Rodin / Sketchfab generation feasibility as a modeling shortcut: **both disabled**
    (`get_hyper3d_status`/`get_sketchfab_status` checked live). Both need a one-time LOCAL setup (a
    checkbox in Blender's own UI + a Sketchfab API key) — flagged to the director as a parallel,
    non-blocking opportunity, not attempted this round.
  - Launched attempt 3 with the style brief as the primary spec (keep the beacon/D11 meaning, redo
    geometry/shading/materials to the flat-toon convention). Explicitly briefed the evidence/QA step to
    only touch files this task created, after the round-1 `git checkout --` scope incident.
- 2026-08-01 **Session wrap-up — attempt 3 running, interrupted once by a session restart, resumed,
  director asked to continue tomorrow.** Attempt 3 round 1 scored 66/95, round 2 scored **56/95**. The
  workflow got interrupted mid-round-3 (session restart, right as the lead started scoring) before its
  score was captured — logged at the time as "looks materially stronger," which turned out to be
  half-right (see below).
- 2026-08-01 **Resumed a second time after another session restart interruption. Now have real
  round 3 and round 4 scores — both landed at 53/95.** Corrects the previous entry's optimism: round 3
  did root-cause the color-clipping "halo" for real (it was overlapping decal geometry rendering as a
  blown-white blend in Cycles, not lighting) and rewrote the tautological verifier — genuine fixes — but
  the lead still scored it lower than round 1 (53 vs 66), because fixing those exposed/introduced new
  problems: a floating disconnected category-stud ring, an unresolved footprint that exceeds the design
  system's grid-cell bracket, and a CVD-accessibility fallback that isn't actually legible. Round 4 fixed
  ALL of round 3's findings (shader rewritten so trim no longer clips, footprint brought inside the
  design-system bracket, stud ring deleted rather than re-patched, garland moved to the eave, connected-
  component check added and a real regression caught mid-round) and still landed at exactly 53 — the
  lead's round-4 review is different findings, not repeats (bunting flags occluded/edge-on for the third
  attempt running, a shared "shadow" ink that isn't actually a value-band of each surface's own hue so
  the wall hue vanishes to 0 pixels from some camera angles, bevels reading as a thin outline rather than
  real roundness). **Score trajectory this attempt: 66 → 56 → 53 → 53 — declining then flat, not
  converging.** Two consecutive identical scores (53, 53) is one round short of this loop's own
  no-progress brake (3 rounds within ±2 stops it automatically). Round 5 (the last one this attempt gets)
  was resumed via `resumeFromRunId: 'wf_4d73e7d7-d50'` and is running now — genuinely likely to be the
  last round regardless of outcome (max-rounds limit either way). **Combined with attempts 1-2: this is
  now 15 rounds and roughly 4-5M tokens spent on one landmark prop.** When round 5 lands, report the
  outcome to the director plainly either way — if it still hasn't passed, this needs a real conversation
  about whether continued rounds are the right lever at all (the recurring pattern — real, verified fixes
  each round, score not improving — suggests the 95 bar or the whole hand-scripted-primitives approach
  may be the actual constraint, not execution quality).

- 2026-08-02 **Attempt 3 exhausted its 5 rounds. FINAL: 60/95. THIRD escalation on this task overall.**
  Full attempt-3 trajectory: 66 → 56 → 53 → 53 → **60**. Round 5's fixes were real (locked/pre-achievement
  state was previously a byte-for-byte no-op — the Emission swap silently had no effect on the render;
  fixed and verified pixel-different this time) but new/residual issues kept the score well short: the
  plaza's top plane reads as in-shadow while the roof uses a different lighting logic (inconsistent value
  system across surface types), category flags resolve to an unreadable ~30x37px smear at village-camera
  scale, the CVD analysis' own closest-pair claim doesn't reproduce under an independent color-matrix
  check, and the dominant mass is still "featureless box + smaller box + dome."
  - **Totals across all 3 attempts: 15 rounds, ~6.3M subagent tokens** (attempt1 875K + attempt2 2.08M +
    attempt3 ~3.3M across its two resumed invocations), best score ever reached = attempt 2's 66/95.
  - **Pattern across 15 rounds**: the art lead's reviews are consistently rigorous and each round's fixes
    are consistently real and verified (not cosmetic) — this is not a case of the team not trying or not
    listening to feedback. But the score has never trended upward for more than 2 consecutive rounds in
    any attempt, and closing a batch of findings reliably surfaces a new batch worth a similar number of
    points. That pattern — genuine effort, no convergence — is the signal that more rounds of the same
    approach are unlikely to close a 35-point gap.
  - **NOT relaunching a 4th attempt.** This is now the loop's third stop on the exact task per
    `VISION.md` §5's 5-round policy, applied three times running. Reported to the director with the full
    score history and a plain recommendation to decide the next lever (lower the bar for this specific
    task, invest in the Sketchfab/PolyHaven local setup as a base-mesh source instead of continuing
    hand-scripted primitives, accept attempt 2's 66/95 build as a placeholder and move on, or something
    else) rather than another round of the same pattern.

- 2026-08-02 **Root-cause analysis given to the director on request** (5 causes: a broken/self-fooling
  verifier recurred 3x; the flat/toon rendering approach kept partially reverting to lit PBR node
  behavior; fixing one bug made the next one visible for the first time (not a real regression); A5
  originality is a structural ceiling for hand-scripted primitives specifically; 95 is a very high bar —
  best-ever score in 15 rounds was 66). **Director's decision: "2번으로 진행해줘. 기준은 90점으로
  낮출게."** — pursue AI/asset-generation tooling (option 2) AND lower passMark to 90 (the standard
  Gate 2 bar, VISION.md §3.2 — no longer an elevated bar for this task).
  - **Enabled PolyHaven live** (`blendermcp_use_polyhaven` — free, no API key, done immediately):
    521 CC0 models across props/decorative/structures/buildings/lighting/vases. Directly targets the
    recurring A5 "just primitives" deduction — grafting one real downloaded model as the landmark's
    signature accent piece (not the whole build) is a structural fix, not another shading round.
  - **Hyper3D (Rodin) and Sketchfab remain disabled** — both need an API key the PM does not have and
    cannot generate (Sketchfab: free account+token; Hyper3D: likely paid/credit account). Flagged to the
    director as a further option if they want to supply a key later — not blocking this round.
  - Launched attempt 4: agent = ui-ux, passMark = 90, PolyHaven available and recommended for the
    signature accent piece, everything else carried over from attempt 3's brief.
  - **Same day, follow-up: director supplied a free Sketchfab API key — enabled and confirmed live**
    (`get_sketchfab_status` → logged in, ready). Key set via `execute_blender_code` on the scene
    property directly, never written to any file in this repo (verified with a grep sweep — same
    secret-handling discipline as `.discord/config.json`). **Hyper3D explicitly declined by the
    director** ("유료라서 사용하지 못할 것 같아") — not a technical gap, a deliberate skip; don't revisit
    unless the director raises it again. Sketchfab wasn't retrofitted into the already-running attempt 4
    (its brief told the agent Sketchfab was disabled, and re-briefing mid-run isn't worth it) — it's
    available for attempt 5 if attempt 4 doesn't clear 90.

- 2026-08-02 **Attempt 4 exhausted its 5 rounds. FINAL: 65/90. FOURTH escalation on this task.** Score
  trajectory: 60 → 60 → 63 → **68** → 65 (peaked round 4, dropped on round 5 despite real fixes — the
  same whack-a-mole pattern as attempt 3). **Totals now: 4 full attempts, 20 rounds, ~8.8M subagent
  tokens.** Best single-round score ever: 68/90 (this attempt's round 4).
  - **Real progress this attempt**: the PolyHaven vase genuinely broke the "just primitives" ceiling —
    independently verified as real sculpted geometry (10,296 source faces decimated to a clean 501-poly
    shell), recolored into the flat palette, and it visibly reads as a non-primitive signature element in
    the later rounds. A5 originality scores were consistently higher this attempt than any prior one.
  - **New, more structural problem surfaced**: round 5's lead review found the delivered color palette
    drifted outside `00-art-design-system.md`'s own token values into the exact territory that document
    explicitly warns against ("risk is drifting into neon-dark") — e.g. the roof's saturation/value are
    materially different from `color.secondary`, not just a stylistic recolor. Also found: the design
    system's mandated 3-tier face-shading ladder (top/front/side get distinct treatments) shipped as only
    2 tiers, and a `snap_render_to_palette()` post-process step reintroduced a tautological verifier for
    the third time across all attempts (99.4% of pixels forced to the nearest authored hex before the
    audit ran, so the audit could not fail).
  - **Genuinely new accessibility finding**: every render across all 4 attempts has used a pure-black
    background. Round 5 tested trim color against the actual pale village ground color (from the design
    system) for the first time and found it measures 1.00:1 contrast — the landmark's outline would be
    nearly invisible against the real backdrop it will actually sit on. This was never caught before
    because nothing had tested against anything but black.
  - **NOT relaunching a 5th attempt without the director.** This is the fourth stop on the same task.
    Recommend the director look at the actual renders directly (attached) rather than only the score —
    at 8.8M tokens and 20 rounds spent, continuing to chase a number may cost more than it's worth, and
    a numeric gate isn't the only way to decide "good enough" here.
